node_modules
.env*
.eve
.vercel
.workflow-data
.next
.output
.nitro
dist
.DS_Store
*.tsbuildinfo
/.swc

# local ticket data (legacy file store)
.data
.data/

# env is documented in the README, not a committed template (covered by .env* above too)
.env.example
